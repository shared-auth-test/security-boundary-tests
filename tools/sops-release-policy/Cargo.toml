[package]
name = "shared-auth-sops-release-policy"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Fail-closed SOPS release-recipient policy validator"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"

[dev-dependencies]
tempfile = "3"

[workspace]
